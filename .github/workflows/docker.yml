name: Docker

on:
  workflow_dispatch:
  push:
    tags: ["v*"]
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

env:
  # 替换为你的 Docker Hub 镜像名
  IMAGE_NAME: laoknas/mediaplayer

jobs:
  docker:
    name: Build & Push
    runs-on: ubuntu-latest
    # 只有当提交信息中包含 release: v 关键字（忽略大小写），或者是打 tag(v*)，或者是 Pull Request 时才执行
    if: >
      github.event_name == 'pull_request' || 
      startsWith(github.ref, 'refs/tags/') || 
      contains(github.event.head_commit.message, 'release: v')
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version
        id: extract_version
        run: |
          # 优先从 git tag 提取版本号（如 refs/tags/v1.2.3 → 1.2.3）
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${{ github.ref_name }}"
            echo "VERSION=${VERSION#v}" >> $GITHUB_OUTPUT
            echo "Extracted version from tag: ${VERSION#v}"
          else
            # 尝试从提交信息中提取 release: v 后面的版本号
            COMMIT_MSG="${{ github.event.head_commit.message }}"
            echo "Commit message: $COMMIT_MSG"
            VERSION=$(echo "$COMMIT_MSG" | grep -oP '(?<=release: v)[0-9a-zA-Z.-]+' | head -1 || true)
            echo "Extracted version: $VERSION"
            if [ -n "$VERSION" ]; then
              echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
            else
              echo "WARNING: Could not extract version from commit message"
            fi
          fi

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/') }}
            type=raw,value=${{ steps.extract_version.outputs.VERSION }},enable=${{ steps.extract_version.outputs.VERSION != '' }}
          labels: |
            org.opencontainers.image.version=${{ steps.extract_version.outputs.VERSION || 'latest' }}
            org.opencontainers.image.description=智能电视服务端
      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: backend
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            VERSION=${{ steps.extract_version.outputs.VERSION || github.sha }}
